type Heading @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  detail: String!
  route: String!
}

type Project @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  description: String!
  source: AWSURL
  demo: AWSURL
}
